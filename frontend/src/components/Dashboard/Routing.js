import React from 'react'
import {BrowserRouter as Router, Route, Switch} from 'react-router-dom'

const Routing = () => {
    return (
        <Router>
            <Route path="/setting" exact component={}></Route>
        </Router>
    )
}

export default Routing
